<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </coverage>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <!-- Isolated MySQL test database — mirrors production engine, never the dev DB. -->
        <!-- Credentials come from .env (local) or .env.testing (CI) — never hardcoded here. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="ultimatepos_test"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- Keep Passport from requiring generated keys in tests (we use actingAs). -->
        <env name="PASSPORT_PERSONAL_ACCESS_CLIENT_ID" value="1"/>
        <env name="PASSPORT_PERSONAL_ACCESS_CLIENT_SECRET" value="test-secret"/>
    </php>
</phpunit>
